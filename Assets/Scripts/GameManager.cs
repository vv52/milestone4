using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Tree.cs;

public class GameManager : MonoBehaviour
{
	public Tree questionTree;

    // Start is called before the first frame update
    void Start()
    {
        BuildTree();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void BuildTree()
    {
    	while ()
    }
}
